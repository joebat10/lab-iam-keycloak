# Copie en .env, puis génère des secrets aléatoires avec :
#   openssl rand -base64 32

LLDAP_JWT_SECRET=replace-with-openssl-rand-base64-32
LLDAP_KEY_SEED=replace-with-another-openssl-rand-base64-32
LLDAP_ADMIN_PASSWORD=change-me-lldap-admin
LLDAP_BASE_DN=dc=lab-iam,dc=local
